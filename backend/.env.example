# ------------------------------------------------------------------
# Server
# ------------------------------------------------------------------
NODE_ENV=development
PORT=5001
CLIENT_ORIGIN=http://localhost:5173

# ------------------------------------------------------------------
# MySQL Database
# ------------------------------------------------------------------
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=bank_statement_converter
DB_CONNECTION_LIMIT=10

# ------------------------------------------------------------------
# JWT (authentication foundation - full auth routes arrive in Phase 2)
# ------------------------------------------------------------------
JWT_SECRET=replace_this_with_a_long_random_string
JWT_EXPIRES_IN=7d
COOKIE_MAX_AGE_DAYS=7

# ------------------------------------------------------------------
# File uploads (used from Phase 3 onward, declared now so .env is stable)
# ------------------------------------------------------------------
UPLOAD_TEMP_DIR=./tmp/uploads
MAX_UPLOAD_SIZE_MB=15
